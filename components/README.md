# Components

Componentes reutilizaveis entre areas do projeto.

- `ui`: blocos visuais genericos.
- `layout`: estruturas de pagina e composicao.
- `shared`: componentes compartilhados entre features.

Componentes especificos devem ficar dentro da feature correspondente.
